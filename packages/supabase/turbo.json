{
    "extends": [
        "//"
    ],
    "tasks": {
        "build": {
            "dependsOn": [
                "@makify/supabase#gen:types:local"
            ]
        },
        "gen:types:local": {
            "inputs": [
                "./**"
            ],
            "env": [
                "VERCEL_ENV"
            ]
        }
    }
}