{
    "extends": [
        "//"
    ],
    "tasks": {
        "build": {
            "dependsOn": [
                "@makify/supabase#gen:types:local"
            ]
        },
        "gen:types:local": {
            "inputs": [
                "src/supabase/**/*.sql",
                "src/supabase/**/*.toml"
            ]
        }
    }
}